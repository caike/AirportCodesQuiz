import React from "react";
import ReactDOM from "react-dom";

class Quiz extends React.Component {
  render(){
    return (<div>I am the quiz</div>);
  }
}

ReactDOM.render( <Quiz />, document.getElementById("container") );

